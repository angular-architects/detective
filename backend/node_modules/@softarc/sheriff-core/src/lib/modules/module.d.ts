import UnassignedFileInfo from '../file-info/unassigned-file-info';
import { FileInfo } from './file.info';
import { FsPath } from '../file-info/fs-path';
export declare class Module {
    readonly path: FsPath;
    private readonly fileInfoMap;
    private readonly getFileInfo;
    readonly isRoot: boolean;
    readonly directory: FsPath;
    fileInfos: FileInfo[];
    constructor(path: FsPath, fileInfoMap: Map<FsPath, FileInfo>, getFileInfo: (fsPath: FsPath) => FileInfo, isRoot: boolean);
    addFileInfo(unassignedFileInfo: UnassignedFileInfo): void;
}
