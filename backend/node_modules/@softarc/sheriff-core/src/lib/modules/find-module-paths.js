"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.findModulePaths = void 0;
const getFs_1 = __importDefault(require("../fs/getFs"));
const log_1 = require("../log");
const log = (0, log_1.logger)('core.modules.find-path');
const findModulePaths = (projectDirs) => {
    const fs = (0, getFs_1.default)();
    let modules = [];
    for (const projectDir of projectDirs) {
        modules = modules.concat(fs.findFiles(projectDir, 'index.ts'));
    }
    log.info(modules.join(', '));
    return new Set(modules);
};
exports.findModulePaths = findModulePaths;
//# sourceMappingURL=find-module-paths.js.map