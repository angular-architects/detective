import { FsPath } from '../file-info/fs-path';
import { FileInfo } from '../modules/file.info';
import { TsData } from '../file-info/ts-data';
import { Module } from '../modules/module';
export type ParsedResult = {
    fileInfo: FileInfo;
    modules: Module[];
    rootDir: FsPath;
    getFileInfo: (path: FsPath) => FileInfo;
};
export declare const parseProject: (entryFile: FsPath, traverse: boolean, tsData: TsData, fileContent?: string) => ParsedResult;
