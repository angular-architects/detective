"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.init = init;
const getFs_1 = __importDefault(require("../fs/getFs"));
const cli_1 = require("./cli");
function init() {
    const fs = (0, getFs_1.default)();
    if (fs.exists('sheriff.config.ts')) {
        cli_1.cli.log('');
        cli_1.cli.log('sheriff.config.ts does already exist. Aborting...');
        cli_1.cli.log('');
        cli_1.cli.endProcessOk();
    }
    fs.writeFile('sheriff.config.ts', `\
import { SheriffConfig } from '@softarc/sheriff-core';

/**
  * Minimal configuration for Sheriff
  * Assigns the 'noTag' tag to all modules and
  * allows all modules to depend on each other.
  */

export const config: SheriffConfig = {
  tagging: {}, // apply tags to your modules
  depRules: {
    // root is a virtual module, which contains all files not being part
    // of any module, e.g. application shell, main.ts, etc.
    'root': 'noTag',
    'noTag': 'noTag',

    // add your dependency rules here
  },
};
`);
    cli_1.cli.log('');
    cli_1.cli.log('sheriff.config.ts has been created successfully.');
    cli_1.cli.log('');
    cli_1.cli.endProcessOk();
}
//# sourceMappingURL=init.js.map