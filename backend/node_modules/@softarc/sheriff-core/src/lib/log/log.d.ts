import { LogLevel } from './log-level';
/**
 * resets the logger. only used in tests.
 */
export declare const reset: () => void;
export declare function log(message: string, scope: string | undefined, level: LogLevel): void;
export declare function doLog(data: string): void;
