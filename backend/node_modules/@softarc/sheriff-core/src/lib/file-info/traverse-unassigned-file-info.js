"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.traverseUnassignedFileInfo = traverseUnassignedFileInfo;
function* traverseUnassignedFileInfo(fileInfo) {
    const traversed = new Set();
    function* traverse(fileInfo, level = 1) {
        if (traversed.has(fileInfo.path)) {
            return;
        }
        traversed.add(fileInfo.path);
        yield { fileInfo, level };
        for (const child of fileInfo.imports) {
            yield* traverse(child, level + 1);
        }
    }
    yield* traverse(fileInfo);
}
exports.default = traverseUnassignedFileInfo;
//# sourceMappingURL=traverse-unassigned-file-info.js.map